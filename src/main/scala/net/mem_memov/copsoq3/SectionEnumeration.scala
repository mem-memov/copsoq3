package net.mem_memov.copsoq3

enum SectionEnumeration(section: Section):
  case TrustAndJustice extends SectionEnumeration(section.TrustAndJustice)
  case NegativeActs extends SectionEnumeration(section.NegativeActs)
  case Health extends SectionEnumeration(section.Health)
